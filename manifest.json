{
  "schemaVersion": 1,
  "id": "io.github.iryzhkov.glide-menus",
  "name": "Glide menus",
  "version": "1.2.0",
  "author": "Igor Ryzhkov",
  "license": "MIT",
  "description": "Cantina's cascading context menus reworked around clean, centered motion: the pane in use stays mid-screen, ghost panes preview submenus before you enter, the selection glides between rows, and typing searches the whole menu tree.",
  "kinds": [
    "menu",
    "bar-widget"
  ],
  "keepLoaded": true,
  "entryPoints": {
    "menu": "ContextMenu.qml",
    "barWidget": "BarWidget.qml"
  },
  "omarchy": {
    "clonedFrom": "omarchy.menu"
  },
  "barWidget": {
    "displayName": "Glide menu",
    "description": "Cascading Omarchy menu with expressive motion, on the bar button and the desktop right-click",
    "category": "Compositor",
    "allowMultiple": false,
    "defaultSection": "left",
    "defaults": {
      "desktopRightClick": true,
      "wallpaperDoubleClick": true,
      "inlineApps": true,
      "submenuDelay": 140,
      "paneWidth": 300,
      "rightClickCommand": "xdg-terminal-exec",
      "animations": true,
      "summonPlacement": "center",
      "escClosesAll": true,
      "hoverSelects": false,
      "layoutStyle": "centered",
      "appsShown": 12
    },
    "schema": [
      {
        "key": "animations",
        "type": "boolean",
        "label": "Animations",
        "defaultValue": true,
        "description": "Pane entrances, the sliding centered chain, ghost previews, and the gliding selection highlight. Off restores the instant behavior of the original context menus."
      },
      {
        "key": "layoutStyle",
        "type": "enum",
        "label": "Cascade layout",
        "options": [
          "centered",
          "anchored"
        ],
        "defaultValue": "centered",
        "description": "\"centered\" keeps the pane you are using mid-screen and slides ancestors off to the right, so your eyes never travel. \"anchored\" is the original cascade growing sideways from the summon point."
      },
      {
        "key": "summonPlacement",
        "type": "enum",
        "label": "Keybinding opens the menu",
        "options": [
          "center",
          "pointer"
        ],
        "defaultValue": "center",
        "description": "Where a keybinding or IPC summon places the menu in the anchored layout: mid-screen or under the pointer. The centered layout always opens mid-screen, and the desktop right-click always opens at the click."
      },
      {
        "key": "escClosesAll",
        "type": "boolean",
        "label": "Escape closes the whole menu",
        "defaultValue": true,
        "description": "Escape dismisses the entire cascade. Off makes Escape walk back one pane at a time (clearing the type-ahead first), like the original context menus; Left arrow and right-click always walk back."
      },
      {
        "key": "hoverSelects",
        "type": "boolean",
        "label": "Hover moves the selection",
        "defaultValue": false,
        "description": "Pointer hover moves the selection and opens submenus after the dwell delay. Off (the default) leaves selection to the keyboard and opens submenus only on click."
      },
      {
        "key": "desktopRightClick",
        "type": "boolean",
        "label": "Right-click the desktop to open the menu",
        "defaultValue": true,
        "description": "Puts a transparent catcher above the wallpaper and below every window, so a right-click on bare desktop opens the menu under the pointer. Off leaves the desktop to whatever else wants it, and the bar button and the keybinding still work."
      },
      {
        "key": "wallpaperDoubleClick",
        "type": "boolean",
        "label": "Double-click the desktop for the wallpaper picker",
        "defaultValue": true,
        "description": "The catcher takes the clicks Omarchy's background plugin would otherwise get, so it hands this one back: double-click still opens omarchy-theme-bg-switcher. Only matters while the setting above is on."
      },
      {
        "key": "inlineApps",
        "type": "boolean",
        "label": "List applications in the menu",
        "defaultValue": true,
        "description": "Fills the Apps submenu in from the same library the launcher reads, icons and all, and type to narrow it. Off sends Apps to the built-in Omarchy menu instead, which searches rather than scrolls."
      },
      {
        "key": "submenuDelay",
        "type": "integer",
        "label": "Open a submenu after",
        "min": 0,
        "max": 600,
        "step": 20,
        "defaultValue": 140,
        "description": "Milliseconds the pointer has to rest on a row before its submenu opens. Long enough that a diagonal sweep toward an already-open submenu does not tear it down on the way past; short enough not to feel like waiting. 0 opens instantly."
      },
      {
        "key": "paneWidth",
        "type": "integer",
        "label": "Pane width",
        "min": 160,
        "max": 520,
        "step": 4,
        "defaultValue": 300,
        "description": "In the shell's spacing units, so it tracks the rest of your scaling. Wider suits long application names; narrower keeps a deep cascade on one screen."
      },
      {
        "key": "rightClickCommand",
        "type": "string",
        "label": "Right-click on the bar button runs",
        "defaultValue": "xdg-terminal-exec",
        "description": "What a right-click on the bar button does. Matches the built-in Omarchy menu button, which opens a terminal. Empty to do nothing."
      },
      {
        "key": "appsShown",
        "type": "integer",
        "label": "Apps shown before search",
        "min": 0,
        "max": 40,
        "step": 1,
        "defaultValue": 12,
        "description": "Provider-filled submenus (Apps, fonts) list only this many top entries, ranked the way the launcher ranks them, with a hint row for the rest \u2014 typing searches the full set. 0 shows everything and scrolls."
      }
    ]
  }
}
